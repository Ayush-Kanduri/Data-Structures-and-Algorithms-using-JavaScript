//Floating poitn numbers addition can result into extreme decimal values. There are some rounding errors and we don't get the exact result.
console.log(0.1 + 0.2);
//JS converts 0.1 to binary decimal number which is infinitely long number and same is for 0.2.
